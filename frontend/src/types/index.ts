export interface DetectResult {
  plate: string;
  image_path: string;
  image_url: string;
  created_at: string;
}

export interface LogItem {
  plate: string;
  image_path: string;
  image_url: string;
  created_at: string;
}
